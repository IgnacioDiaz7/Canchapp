import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GymMunQuilicuraPage } from './gym-mun-quilicura.page';

describe('GymMunQuilicuraPage', () => {
  let component: GymMunQuilicuraPage;
  let fixture: ComponentFixture<GymMunQuilicuraPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GymMunQuilicuraPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
